/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Modelo;

/**
 *
 * @author Wilmer Quito
 */
public class Validaciones {

    /*public void Valida(KeyEvent evt) {
        char car = evt.getKeyChar();

        //LONGITUD
        if (txtBuscar.getText().length() >= 50) {
            evt.consume();
        }

        //SOLO LETRAS
        if (((car < 'A') || (car > 'Z')) && (((car < 'a') || (car > 'z')))) {
            evt.consume();
        }

        //SOLO NUMERO ENTERO
        if ((car < '0') || (car > '9')) {
            evt.consume();
        }

        //SOLO NUMERO DECIMAL CON UN ESPACIO
        if ((evt.VK_PERIOD != car) && (car < '0') || (car > '9')) {
            evt.consume();
        }
        if (evt.VK_PERIOD == car && txtPrecio.getText().contains(".")) {
            evt.consume();
        }

        //SOLO NUMEROS Y LETRAS
        if ((car < '0') || (car > '9') && (car < 'A') || (car > 'Z') && (car < 'a') || (car > 'z')) {
            evt.consume();
        }

        //SOLO LETRAS CON ESPACIO
        if (((car < 'A') || (car > 'Z')) && (((car < 'a') || (car > 'z'))) && (evt.VK_SPACE != car)) {
            evt.consume();
        }

        //SOLO NUMEROS Y LETRAS CON ESPACIOS
        if ((evt.VK_SPACE != car) && (car < '0') || (car > '9') && (car < 'A') || (car > 'Z') && (car < 'a') || (car > 'z')) {
            evt.consume();
        }
    }*/
}
